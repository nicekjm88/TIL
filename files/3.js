
    let newArr = ['바보', '멍튱이', 'ㅄ'];
    let newObj = {};

    function arrayLastItem(data) {
        return Array.isArray(data) ? data[data.length - 1] : console.log('에러');
    }
    
    console.log(arrayLastItem(newObj));


    Condition ? true : false;